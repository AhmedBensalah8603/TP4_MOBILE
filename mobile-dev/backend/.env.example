# Base URL clients will use to reach this service (include scheme and optional path)
# e.g. http://localhost:8000 or https://my-backend.example
PUBLIC_BASE_URL=http://localhost:8000

# Azure TTS (optional) — provide to enable server-side TTS generation
AZURE_TTS_KEY=
AZURE_TTS_REGION=

# Azure OpenAI / LLM (optional) — used if you enable LLM calls
# Example endpoint: https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=
# API key (set via Key Vault / env vars in production)
AZURE_OPENAI_KEY=
# Deployment name (model deployment id you created in Azure OpenAI)
AZURE_OPENAI_DEPLOYMENT=
